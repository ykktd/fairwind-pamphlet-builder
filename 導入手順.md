# 導入手順（管理者向け）

担当者がGitHub Pagesの共通URLからGoogle ドライブを使えるようにするための初回設定です。Google CloudプロジェクトとGitHubリポジトリを用意します。

## 1. Google Cloudを設定する

1. Google Cloudでプロジェクトを作り、**Google Drive API**と**Google Picker API**を有効にする。
2. OAuth同意画面を設定する。サークルで使うGoogleアカウントをテストユーザーに追加する。公開状態を「テスト」にする場合、登録したユーザーだけが接続できます。
3. 「ウェブ アプリケーション」型のOAuthクライアントIDを作る。承認済みのJavaScript生成元に、公開予定の **`https://〈所有者〉.github.io`** と、開発で使う場合は `http://localhost:5173` または `http://127.0.0.1:5173` を登録する。OAuthの生成元にはリポジトリ名のパスを含めない。
4. APIキーを作り、アプリケーション制限を「ウェブサイト」にする。許可する参照元に **`https://〈所有者〉.github.io/〈リポジトリ名〉/*`** と `https://docs.google.com/*` を入れる。API制限はGoogle Picker APIとGoogle Drive APIにする。
5. Google Cloudの「IAMと管理 → 設定」に表示される**プロジェクト番号**を控える。これはPickerのApp IDとして使う。

Googleの[Picker設定例](https://developers.google.com/workspace/drive/picker/guides/web-picker-sample)は、上記2つのAPI、APIキー、OAuthクライアントID、プロジェクト番号を同じCloudプロジェクトで使うよう案内しています。このツールのOAuth権限は、Pickerで選んだファイルやアプリが作ったファイルに限る `drive.file` です。[Googleの権限説明](https://developers.google.com/workspace/drive/api/guides/api-specific-auth)を参照してください。

## 2. GitHubリポジトリを用意する

サークルのGitHubアカウントまたは組織に、このツール用のリポジトリを作り、このフォルダのアプリ用ファイルを入れます。GitHub Freeでは公開リポジトリでPagesを利用できます。公開リポジトリの内容は誰でも読めるため、企画資料やプロフィールPDFは入れず、Google ドライブに置いてください。元の作成手順書や企画書などは `.gitignore` で除外していますが、初回の公開前に登録するファイルを確認してください。

リポジトリの **Settings → Secrets and variables → Actions → Variables** に、次の4つを登録します。これらはサイトのコードに含まれる公開識別子です。パスワードやサービスアカウントの秘密鍵は登録しません。

| 変数名 | 値 |
| --- | --- |
| `GOOGLE_CLIENT_ID` | ウェブアプリ用OAuthクライアントID |
| `GOOGLE_API_KEY` | 制限済みAPIキー |
| `GOOGLE_APP_ID` | Google Cloudのプロジェクト番号 |
| `COMMON_FOLDER_ID` | 共通資料フォルダのDrive ID。省略すればツールの初期値を使用 |

## 3. GitHub Pagesに公開する

リポジトリの **Settings → Pages → Build and deployment → Source** で **GitHub Actions** を選びます。このフォルダの [公開ワークフロー](.github/workflows/deploy-pages.yml)が、`main` ブランチへの更新時と手動実行時にテスト、ビルド、公開を行います。成功すると `https://〈所有者〉.github.io/〈リポジトリ名〉/` で開けます。[GitHubのワークフロー手順](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)も参照してください。

公開URLが予定と異なったら、OAuthクライアントIDの承認済み生成元とAPIキーのウェブサイト制限を実際のURLに合わせて更新します。設定反映後に接続を確認します。利用者にサイト自体への閲覧制限が必要な場合は、通常の公開Pagesではなく別の認証方法を検討します。

## 4. 公開後の確認

1. 担当者のGoogleアカウントで接続する。
2. 企画共有フォルダを選び、「パンフ作成用」が作られることを確認する。
3. 共通資料と企画用Google ドキュメントを選び、PDFを生成する。
4. 完成PDFをDriveに保存し、別の担当者のアカウントでも企画を開けるか確認する。
5. 実際の過去パンフ1件で目次、ページ番号、約70ページの生成時間を確認する。

共有相手が使える資料はDriveの権限に従います。公開したサイトに担当者のGoogleパスワードや資料は保存されません。GoogleのOAuth同意画面をテスト状態にする場合は、[登録したテストユーザーだけが利用できます](https://developers.google.com/identity/protocols/oauth2/production-readiness/overview)。
